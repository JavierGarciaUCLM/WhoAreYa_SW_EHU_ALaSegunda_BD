<!DOCTYPE html>
<!-- Toda la decoración, chatgptado -->
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .login-container {
            background: white;
            padding: 3rem;
            border-radius: 8px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.2);
            width: 100%;
            max-width: 400px;
        }
        .login-container h1 {
            margin-bottom: 2rem;
            text-align: center;
            color: #333;
        }
        .form-group {
            margin-bottom: 1.5rem;
        }
        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 600;
            color: #555;
        }
        .form-group input {
            width: 100%;
            padding: 0.75rem;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-size: 1rem;
        }
        .form-group input:focus {
            outline: none;
            border-color: #667eea;
        }
        .btn {
            width: 100%;
            padding: 0.75rem;
            background: #667eea;
            color: white;
            border: none;
            border-radius: 4px;
            font-size: 1rem;
            cursor: pointer;
            transition: background 0.2s;
        }
        .btn:hover {
            background: #5568d3;
        }
        .alert {
            padding: 1rem;
            border-radius: 4px;
            margin-bottom: 1rem;
            background: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h1>Panel de Administración</h1>
        <% if (error) { %>
            <div class="alert"><%= error %></div>
        <% } %>
        <form method="POST" action="/admin/login">
            <div class="form-group">
                <label for="email">Email</label>
                <input type="email" id="email" name="email" required>
            </div>
            <div class="form-group">
                <label for="password">Contraseña</label>
                <input type="password" id="password" name="password" required>
            </div>
            <button type="submit" class="btn">Iniciar Sesión</button>
        </form>
        <!-- OAuth google github-->
        <% if (typeof googleEnabled !== 'undefined' && googleEnabled || typeof githubEnabled !== 'undefined' && githubEnabled) { %>
        <div style="margin-top: 1.5rem; text-align: center;">
            <div style="margin: 1rem 0; color: #666; font-size: 0.9rem;">o</div>
            
            <% if (typeof googleEnabled !== 'undefined' && googleEnabled) { %>
            <a href="/api/auth/google" style="display: block; width: 100%; padding: 0.75rem; background: #4285f4; color: white; text-decoration: none; border-radius: 4px; margin-bottom: 0.75rem; text-align: center; font-size: 0.95rem;">
                <span style="font-weight: 600;">🔵 Log in with Google</span>
            </a>
            <% } %>
            
            <% if (typeof githubEnabled !== 'undefined' && githubEnabled) { %>
            <a href="/api/auth/github" style="display: block; width: 100%; padding: 0.75rem; background: #24292e; color: white; text-decoration: none; border-radius: 4px; text-align: center; font-size: 0.95rem;">
                <span style="font-weight: 600;">⚫ Log in with GitHub</span>
            </a>
            <% } %>
        </div>
        <% } %>
    </div>
</body>
</html>

